import styles from './Header.module.css'

export const Header = () => {
    return (
        <section className={styles.header}>
            <p>
                Maps Test
            </p>
        </section>
    );
};